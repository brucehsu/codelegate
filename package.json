{
  "name": "codelegate",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:desktop": "pnpm --filter @codelegate/desktop build",
    "clean": "tsc -b --clean",
    "dev:desktop": "pnpm --filter @codelegate/desktop dev",
    "tauri:desktop": "pnpm --filter @codelegate/desktop tauri",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
